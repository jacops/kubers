---
kind: pipeline
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: test
  image: golang:1.14
  commands:
  - make test-coverage
  - make build
  - bash <(curl -s https://codecov.io/bash)
